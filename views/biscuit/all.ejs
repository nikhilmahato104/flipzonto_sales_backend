<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>All Biscuits</title>
  
  <style>
    h1 {
      text-align: center;
      color: #000000;
      margin-bottom: 1.5rem;
    }

    .biscuit-list {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      padding: 1rem;
      max-width: 600px;
      margin: auto;
    }

    .biscuit-card {
      background-color: #f0f7ff;
      border: 1px solid #ddd;
      border-radius: 10px;
      padding: 1rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
    }

    .biscuit-img {
      border-radius: 6px;
      margin-right: 1rem;
    }

    .biscuit-info {
      flex: 1;
      margin-left: 1rem;
    }

    .biscuit-name {
      font-size: 1.1rem;
      color: #1e3a8a;
    }

    .biscuit-actions {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      align-items: center;
      min-width: 100px;
    }

    .edit-btn,
    .delete-btn {
      width: 100px;
      height: 36px;
      font-size: 0.9rem;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      text-align: center;
      text-decoration: none;
      cursor: pointer;
      display: inline-flex;
      justify-content: center;
      align-items: center;
      transition: 0.3s ease;
    }

    .edit-btn {
      background-color: #1e3a8a;
      color: white;
    }

    .edit-btn:hover {
      background-color: #16307a;
    }

    .delete-btn {
      background-color: #e53935;
      color: white;
    }

    .delete-btn:hover {
      background-color: #c62828;
    }

    @media (max-width: 600px) {
      .biscuit-card {
        flex-direction: column;
        align-items: flex-start;
      }

      .biscuit-actions {
        flex-direction: row;
        gap: 1rem;
        margin-top: 0.5rem;
      }
    }

    .add-btn-wrapper {
      display: flex;
      justify-content: center;
      margin: 1.5rem 0;
    }

    .add-btn {
      background-color: #1e3a8a;
      color: white;
      font-size: 1rem;
      padding: 0.6rem 1.5rem;
      border: none;
      border-radius: 8px;
      text-decoration: none;
      font-weight: bold;
      transition: background-color 0.3s ease;
    }

    .add-btn:hover {
      background-color: #16307a;
    }
    .add-btn1{
      background-color: #c5cce0;
      color: rgb(0, 0, 0);
      font-size: 1rem;
      padding: 0.6rem 1.5rem;
      border: none;
      border-radius: 8px;
      text-decoration: none;
      font-weight: bold;
      
    }
  </style>
</head>
<body>
  <h1 class="biscuit-heading">All Biscuits</h1>
<a href="/" class="add-btn1">🔙 Go to Dashboard</a>

  <div class="add-btn-wrapper">
    <a href="/biscuit/new" class="add-btn">➕ Add Biscuit</a>
  </div>

  <div class="biscuit-list">
    <% biscuits.forEach(biscuit => { %>
      <div class="biscuit-card">
        <img src="<%= biscuit.image %>" width="60" class="biscuit-img" />
        <div class="biscuit-info">
          <strong class="biscuit-name"><%= biscuit.name %></strong>
          <p>MRP: ₹<%= biscuit.MRP %></p>
          <p>Weight: <%= biscuit.total_weight %></p>
          <p>Jars: <%= biscuit.available_jar %></p>
          <p>Rate: ₹<%= biscuit.rate %></p>
        </div>
        <div class="biscuit-actions">
          <a href="/biscuit/edit/<%= biscuit._id %>" class="edit-btn">Edit</a>
          <form method="POST" action="/biscuit/delete/<%= biscuit._id %>">
            <button type="submit" class="delete-btn">🗑️ Delete</button>
          </form>
        </div>
      </div>
    <% }) %>
  </div>
</body>
</html>

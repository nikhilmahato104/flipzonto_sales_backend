<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>All Kirana Items</title>
  <style>
    h1 {
      text-align: center;
      color: #000000;
      margin-bottom: 1.5rem;
    }

    .kirana-list {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      padding: 1rem;
      max-width: 600px;
      margin: auto;
    }

    .kirana-card {
      background-color: #f0f7ff;
      border: 1px solid #ddd;
      border-radius: 10px;
      padding: 1rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
    }

    .kirana-img {
      border-radius: 6px;
      margin-right: 1rem;
    }

    .kirana-info {
      flex: 1;
      margin-left: 1rem;
    }

    .kirana-name {
      font-size: 1.1rem;
      color: #1e3a8a;
    }

    .kirana-actions {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      align-items: center;
      min-width: 100px;
    }

    .edit-btn,
    .delete-btn {
      width: 100px;
      height: 36px;
      font-size: 0.9rem;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      text-align: center;
      text-decoration: none;
      cursor: pointer;
      display: inline-flex;
      justify-content: center;
      align-items: center;
      transition: 0.3s ease;
    }

    .edit-btn {
      background-color: #1e3a8a;
      color: white;
    }

    .edit-btn:hover {
      background-color: #16307a;
    }

    .delete-btn {
      background-color: #e53935;
      color: white;
    }

    .delete-btn:hover {
      background-color: #c62828;
    }

    @media (max-width: 600px) {
      .kirana-card {
        flex-direction: column;
        align-items: flex-start;
      }

      .kirana-actions {
        flex-direction: row;
        gap: 1rem;
        margin-top: 0.5rem;
      }
    }

    .add-btn-wrapper {
      display: flex;
      justify-content: center;
      margin: 1.5rem 0;
    }

    .add-btn {
      background-color: #1e3a8a;
      color: white;
      font-size: 1rem;
      padding: 0.6rem 1.5rem;
      border: none;
      border-radius: 8px;
      text-decoration: none;
      font-weight: bold;
      transition: background-color 0.3s ease;
    }

    .add-btn:hover {
      background-color: #16307a;
    }
  </style>
</head>
<body>
  <h1>All Kirana Items</h1>

  <div class="add-btn-wrapper">
    <a href="/kirana/new" class="add-btn">➕ Add Kirana</a>
  </div>

  <div class="kirana-list">
    <% kiranas.forEach(kirana => { %>
      <div class="kirana-card">
        <img src="<%= kirana.image %>" width="60" class="kirana-img" />
        <div class="kirana-info">
          <strong class="kirana-name"><%= kirana.name %></strong>
          <p>MRP: ₹<%= kirana.MRP %></p>
          <p>Volume: <%= kirana.total_volume %></p>
          <p>Available Jars: <%= kirana.available_jar %></p>
          <p>Rate: ₹<%= kirana.rate %></p>
        </div>
        <div class="kirana-actions">
          <a href="/kirana/edit/<%= kirana._id %>" class="edit-btn">Edit</a>
          <form method="POST" action="/kirana/delete/<%= kirana._id %>">
            <button type="submit" class="delete-btn">🗑️ Delete</button>
          </form>
        </div>
      </div>
    <% }) %>
  </div>
</body>
</html>
